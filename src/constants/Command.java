package twophasecommit.constants;

/**
 * This enum contains a list of commands
 * that the user can utilize
 */
public enum Command {
    TRANSACTION,
    ACCOUNT,
    STATE,
    DATA,
    HELP,
    ID
}